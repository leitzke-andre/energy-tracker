<form method="post" action="{{ path('app_reading_delete', {'id': reading.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reading.id) }}">
    <button class="btn">Delete</button>
</form>
